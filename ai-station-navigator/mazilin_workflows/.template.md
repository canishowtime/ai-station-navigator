---
# AI Station Navigator - 工作流模版 v1.2.0 (精简版)
# 完整配置说明见: README.md

# ===== 必填字段 =====
name:                    # 工作流名称 (如: 深度分析)
version:                 # 版本号 (如: 1.0.0)
description:             # 功能描述 (一句话)
depends_on:              # 依赖技能列表
  - skill-name           # 技能名称需与 .claude/skills/ 目录一致

# ===== 可选字段 (按需添加) =====
tags:                    # 标签 (如: [analysis, fact-check])
timeout_seconds: 600     # 超时时间
sequential: true         # 串行执行
stop_on_error: true       # 失败停止

# ===== 安全配置 (推荐) =====
sandbox:
  file_access:
    read: [mybox/**, docs/**, .claude/**]
    write: [mybox/workspace/**, mybox/temp/**]
    forbidden: [*.exe, *.dll, system32/**]
  network: false          # 禁止网络访问

# ===== 输出配置 =====
output:
  location: inline        # inline | file | both
  format: markdown        # markdown | json | text
  file_path: mybox/workspace/{name}/{date}.md  # 文件输出路径

# ===== 其他可选配置 =====
# input: { types: [], max_length: ... }
# imports: [workflows/shared/组件.md]
# state: { enabled: true, type: file, location: ... }
# metadata: { category: xxx, priority: high }
---

# 工作流标题

**功能**：一句话描述核心功能

**工作流链路**：`技能1 → 技能2 → 最终输出`

**使用方式**：
```
用户：使用 [工作流名称] 分析：[内容]
Kernel：自动执行各阶段 → 对话框输出摘要
```

---

## 使用场景

- **场景1**：具体描述
- **场景2**：具体描述

---

## 工作流详解

### 阶段 1：[技能/步骤名称]

**功能**：此阶段的功能说明

**执行**：使用 `@技能名` 执行任务

**输入**：
- 参数1：说明
- 参数2：说明

**输出**：
- 结果1：说明
- 结果2：说明

**示例**：
```
[展示实际输出格式]
- 关键指标：数值
- 关键发现：xxx
```

---

### 阶段 2：[技能/步骤名称]

**功能**：此阶段的功能说明

**执行**：基于上阶段结果使用 `@技能名`

**输入**：阶段1的输出结果

**输出**：
- 结果1：说明
- 结果2：说明

---

### 阶段 N：最终输出

**功能**：整理并输出结果

**输出格式**：
```markdown
## 核心结果
- 结果1：xxx
- 结果2：xxx

## 详细分析
<details>
<summary>查看完整数据</summary>

[完整分析内容]

</details>
```

---

## 输出位置

| 类型 | 位置 | 说明 |
|------|------|------|
| 对话框 | 直接输出 | 快速查看 |
| 报告 | `mybox/workspace/{name}/{date}.md` | 长期存档 |

---

## 注意事项

1. **串行执行**：禁止并行调用技能
2. **路径引用**：使用引用方式传递路径，禁止嵌入内容
3. **失败停止**：任一步失败即停止询问
4. **遵守限制**：遵守 sandbox 配置

---

## 变更历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| 1.0.0 | YYYY-MM-DD | 初始版本 | Your Name |
