# AI Station Navigator - Python Dependencies
# 安装方式: pip install -r requirements.txt

# =============================================================================
# 核心依赖
# =============================================================================

# 文档数据库 - 技能索引存储
tinydb~=4.8.0    # 允许 4.8.x 补丁更新，拒绝 5.x breaking changes

# YAML 配置解析
pyyaml>=6.0

# Security Scanner for Agent Skills (Apache 2.0)
# 注意：不在 PyPI，需从 GitHub 安装
git+https://github.com/cisco-ai-defense/skill-scanner.git#egg=cisco-ai-skill-scanner
# 注意：该包的 LLM 依赖（google-generativeai, litellm, openai）对本项目非必需
# 安装后可运行：pip uninstall -y google-generativeai google-genai litellm openai google-api-python-client google-auth
# 节省约 180 MB 空间，不影响扫描功能

# YARA 恶意软件模式匹配 (security_scanner.py 依赖)
yara-python>=4.5.0

# Markdown Frontmatter 解析 (security_scanner.py 依赖)
python-frontmatter>=1.0.0

# =============================================================================
# 可选依赖
# =============================================================================

# GitHub 加速（如需要）
# pygithub>=2.0

# 其他工具依赖...
